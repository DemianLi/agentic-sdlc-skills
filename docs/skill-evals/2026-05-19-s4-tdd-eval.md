# Skill Eval — s4-tdd — 2026-05-19

**File**: `skills/s4-tdd/SKILL.md`
**Evaluator**: s0-eval-skill

## Score Summary

| # | Criterion | Score | Evidence |
|---|-----------|-------|----------|
| 1 | 衝突防禦 | ✅ | Lines 227–232: `<supporting-info>` names `/s4-setup-env` (upstream), `TASK_DAG.md` context, and `/s4-impl-task` (downstream) with explicit "Implementer (TDD Mode)" identity |
| 2 | 雙向阻斷 | ✅ | Lines 8–22: `<HARD-GATE>` with 3 hard conditions (test exists, FAIL observed, expected failure reason); Lines 56–70: "Anti-Pattern: Horizontal Slices — FORBIDDEN" with code example; Lines 146–157: "Red Flags" table with 7 concrete rationalizations + reality |
| 3 | 輸入清洗 | ✅ | Lines 81–89: "Input Sanity Check" table explicitly lists 3 required inputs (AC exists, AC is binary, public interface named) with defined behavior for each failure ("Ask:" directive) |
| 4 | 漸進披露 | ✅ | Workflow (lines 73–143) is structured as 5 subsections, each small; no single inline block (code fence, table, subsection body) exceeds 50 lines; Coverage Gate (lines 160–215) is 56 lines but explains 2 modes inline; Checklist (lines 131–142) is 12 lines |
| 5 | 優雅降級 | ✅ | Lines 14–16: explicit rejection ("DELETE IT. Start over. There are NO exceptions."); Lines 176–186: coverage gate provides 3 fallback paths (DONE, DONE_WITH_CONCERNS, BLOCKED); Lines 200–207: brownfield mode provides alternative threshold |
| 6 | 漂移監控 | ❌ | No reference to `tests/fixtures/` or offline eval fixture directory in SKILL.md |

**Total**: 5/6 PASS — NEAR READY

## Defect Details

### ❌ FAIL — Criterion 6: 漂移監控
- **Location**: Entire SKILL.md
- **Defect**: No reference to `tests/fixtures/` directory; no offline test case fixtures to validate TDD enforcement (e.g., valid failing test outputs, horizontal slice anti-patterns, coverage threshold scenarios) across Claude versions
- **Impact**: Cannot verify Iron Law enforcement and TDD workflow remain strict; skill drift undetectable as model capabilities shift

## Recommended Next Step

**Action**: Add fixture reference to `<supporting-info>`
1. Add to `<supporting-info>` (after role identity, line 232): 
   ```
   ## Drift Monitoring
   Evaluation via `tests/fixtures/s4-tdd/`:
   - `valid_red_output.txt` — FAILED test output examples for gate validation
   - `horizontal_slice_example/` — code samples that violate anti-pattern
   - `coverage_threshold_cases/` — greenfield vs. brownfield scenarios
   ```
2. Create fixture files to capture:
   - Expected pytest/jest/go test failure output format
   - Examples of horizontal-slice code to validate rejection
   - Coverage report examples (80%+, 60–79%, <60%) for gate logic

